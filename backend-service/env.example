# ================================
# Environment Variables Example
# ================================
# Copy this file to .env and update the values with your actual configuration
# IMPORTANT: Never commit the .env file to version control!

# ================================
# Application Configuration
# ================================
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8081

# ================================
# Database Configuration
# ================================
# Development Database
DB_URL=jdbc:mysql://localhost:3306/backend-service?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=password

# Production Database (PostgreSQL recommended)
PROD_DB_URL=jdbc:postgresql://localhost:5432/backend-service
PROD_DB_USER=your-production-username
PROD_DB_PASSWORD=your-strong-production-password

# ================================
# JWT Configuration
# ================================
# Generate secure random Base64 keys:
# Linux/Mac: openssl rand -base64 64
# Windows: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_ACCESS_KEY=your-base64-encoded-access-key-here-minimum-64-characters
JWT_REFRESH_KEY=your-base64-encoded-refresh-key-here-minimum-64-characters

# ================================
# SendGrid Email Configuration
# ================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_TEMPLATE_ID=your-sendgrid-template-id

# Email verification link (adjust based on environment)
VERIFICATION_LINK=http://localhost:8081/user/confirm-email

# ================================
# CORS Configuration
# ================================
# Comma-separated list of allowed origins
# Development
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:8080

# Production (replace with your actual domains)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ================================
# Docker Configuration
# ================================
# These are used by docker-compose.yml
COMPOSE_PROJECT_NAME=backend-service

# ================================
# Example Production Configuration
# ================================
# Uncomment and customize for production deployment:
#
# SPRING_PROFILES_ACTIVE=prod
# SERVER_PORT=8081
#
# # Production Database
# PROD_DB_URL=jdbc:postgresql://prod-db-server:5432/backend-service
# PROD_DB_USER=backend_user
# PROD_DB_PASSWORD=your-very-strong-password-here
#
# # Production JWT Keys (use strong, unique keys)
# JWT_ACCESS_KEY=production-access-key-base64-encoded
# JWT_REFRESH_KEY=production-refresh-key-base64-encoded
#
# # Production Email
# SENDGRID_API_KEY=your-production-sendgrid-api-key
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# SENDGRID_TEMPLATE_ID=your-production-template-id
# VERIFICATION_LINK=https://yourdomain.com/user/confirm-email
#
# # Production CORS
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ================================
# Security Notes
# ================================
# 1. Always use strong, unique passwords for production
# 2. Rotate JWT keys periodically
# 3. Use environment-specific SendGrid API keys
# 4. Limit CORS origins to actual domains in production
# 5. Never commit .env file to version control
# 6. Use secret management tools for production (AWS Secrets Manager, HashiCorp Vault, etc.)
